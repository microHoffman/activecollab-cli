# Test fixtures

Fixtures are synthetic, sanitized examples of the ActiveCollab API shapes used
by the supported server build. They contain no production names, task bodies,
email addresses, URLs, IDs, tokens, or attachment contents.

Add a separate version directory only after checking that version's actual API
responses. Prefer reusing the shared API mapping when the contract is compatible.
